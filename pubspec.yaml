name: agro
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  firebase_auth: ^0.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - images/

  uses-material-design: true
